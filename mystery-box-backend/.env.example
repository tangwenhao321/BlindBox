# 复制为 mystery-box-backend/.env 后启动后端：ServerApplication 会在 Spring 启动前加载本文件（不覆盖已有环境变量）
# 与 vipDistributionMall/server/.env 中 REDIS_URL 写法保持一致，便于两项目共用同一 Redis
#
# 无密码示例:
# REDIS_URL=redis://127.0.0.1:6380/0
#
# 有密码示例:
# REDIS_URL=redis://:你的密码@127.0.0.1:6380/0
#
REDIS_URL=redis://127.0.0.1:6380/0

# 本地 MySQL（dev profile / CI）
DEV_DB_PASSWORD=your-mysql-password

# 管理端高危操作二次确认口令（退款审批、CI E2E 等）
ADMIN_ACTION_OTP=change-me-in-production

# 可选：排队首位超时秒数（与移动端 EXPO_PUBLIC_QUEUE_HEAD_TIMEOUT_SEC 对应）
# QUEUE_HEAD_TIMEOUT_SEC=120

# 开发环境允许 mock OTP 000000（生产务必关闭）
# APP_AUTH_ALLOW_MOCK_OTP=true
