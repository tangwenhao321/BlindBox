export io.github.qifan777.server.referral.entity.ReferralCommissionRecord

specification ReferralCommissionRecordSpec {
    #allScalars
    associatedIdEq(user)
    ge(createdTime)
    le(createdTime)
}
