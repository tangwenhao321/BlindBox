export io.github.qifan777.server.refund.entity.RefundRecord

input RefundRecordInput {
    #allScalars(RefundRecord)
    id
}

specification RefundRecordSpec {
    #allScalars
    associatedIdEq(creator)
}