# Production build: API via same-origin /api (see deploy/nginx.conf.example)
VITE_API_PREFIX=/api

# Prefer HttpOnly cookie session (requires same-origin admin+API proxy).
VITE_ADMIN_COOKIE_AUTH=true
