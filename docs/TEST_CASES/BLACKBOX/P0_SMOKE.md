# P0 黑盒执行清单（节选前 200 / 全量 P0=595）

完整 P0 请在 CSV 筛选 `优先级=P0`。

## B-AUTH-0001 用户注册-正常主路径成功
- 模块：认证登录/用户注册
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/register + Register UI 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0002 用户注册-未登录访问被拒绝
- 模块：认证登录/用户注册
- 步骤：1. 不带鉴权调用/打开 POST /front/user/register + Register UI
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0006 用户注册-phone合法手机号成功
- 模块：认证登录/用户注册
- 步骤：1. 输入合法手机号执行 POST /front/user/register + Register UI
- 输入：phone=有效号段
- 预期：通过校验
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0007 用户注册-phone非法格式失败
- 模块：认证登录/用户注册
- 步骤：1. 非法手机号提交
- 输入：phone=123/abcd/空
- 预期：格式错误提示；不继续
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0027 密码登录-正常主路径成功
- 模块：认证登录/密码登录
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/login 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0028 密码登录-未登录访问被拒绝
- 模块：认证登录/密码登录
- 步骤：1. 不带鉴权调用/打开 POST /front/user/login
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0031 密码登录-phone合法手机号成功
- 模块：认证登录/密码登录
- 步骤：1. 输入合法手机号执行 POST /front/user/login
- 输入：phone=有效号段
- 预期：通过校验
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0032 密码登录-phone非法格式失败
- 模块：认证登录/密码登录
- 步骤：1. 非法手机号提交
- 输入：phone=123/abcd/空
- 预期：格式错误提示；不继续
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0040 短信登录-正常主路径成功
- 模块：认证登录/短信登录
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/login/sms 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0041 短信登录-未登录访问被拒绝
- 模块：认证登录/短信登录
- 步骤：1. 不带鉴权调用/打开 POST /front/user/login/sms
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0044 短信登录-phone合法手机号成功
- 模块：认证登录/短信登录
- 步骤：1. 输入合法手机号执行 POST /front/user/login/sms
- 输入：phone=有效号段
- 预期：通过校验
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0045 短信登录-phone非法格式失败
- 模块：认证登录/短信登录
- 步骤：1. 非法手机号提交
- 输入：phone=123/abcd/空
- 预期：格式错误提示；不继续
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0053 发送短信-正常主路径成功
- 模块：认证登录/发送短信
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/auth/sms/send 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0054 发送短信-未登录访问被拒绝
- 模块：认证登录/发送短信
- 步骤：1. 不带鉴权调用/打开 POST /front/auth/sms/send
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0058 发送短信-phone合法手机号成功
- 模块：认证登录/发送短信
- 步骤：1. 输入合法手机号执行 POST /front/auth/sms/send
- 输入：phone=有效号段
- 预期：通过校验
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0059 发送短信-phone非法格式失败
- 模块：认证登录/发送短信
- 步骤：1. 非法手机号提交
- 输入：phone=123/abcd/空
- 预期：格式错误提示；不继续
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0067 修改密码-正常主路径成功
- 模块：认证登录/修改密码
- 步骤：1. 准备合法前置数据 2. 执行 PUT /front/user/password 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0068 修改密码-未登录访问被拒绝
- 模块：认证登录/修改密码
- 步骤：1. 不带鉴权调用/打开 PUT /front/user/password
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0084 Zalo登录-正常主路径成功
- 模块：认证登录/Zalo登录
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/auth/zalo/login 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0085 Zalo登录-未登录访问被拒绝
- 模块：认证登录/Zalo登录
- 步骤：1. 不带鉴权调用/打开 POST /front/auth/zalo/login
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0092 微信注册登录-正常主路径成功
- 模块：认证登录/微信注册登录
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user-we-chat/register 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0093 微信注册登录-未登录访问被拒绝
- 模块：认证登录/微信注册登录
- 步骤：1. 不带鉴权调用/打开 POST /front/user-we-chat/register
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0100 管理端登录-正常主路径成功
- 模块：认证登录/管理端登录
- 步骤：1. 准备合法前置数据 2. 执行 POST /admin/auth/login 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0101 管理端登录-未登录访问被拒绝
- 模块：认证登录/管理端登录
- 步骤：1. 不带鉴权调用/打开 POST /admin/auth/login
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0102 管理端登录-普通用户越权访问管理功能失败
- 模块：认证登录/管理端登录
- 步骤：1. 以用户身份访问 POST /admin/auth/login
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0115 管理端登出-正常主路径成功
- 模块：认证登录/管理端登出
- 步骤：1. 准备合法前置数据 2. 执行 POST /admin/auth/logout 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0116 管理端登出-未登录访问被拒绝
- 模块：认证登录/管理端登出
- 步骤：1. 不带鉴权调用/打开 POST /admin/auth/logout
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0117 管理端登出-普通用户越权访问管理功能失败
- 模块：认证登录/管理端登出
- 步骤：1. 以用户身份访问 POST /admin/auth/logout
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0119 管理端高危授权-正常主路径成功
- 模块：认证登录/管理端高危授权
- 步骤：1. 准备合法前置数据 2. 执行 POST /admin/auth/action-grant 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0120 管理端高危授权-未登录访问被拒绝
- 模块：认证登录/管理端高危授权
- 步骤：1. 不带鉴权调用/打开 POST /admin/auth/action-grant
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-AUTH-0121 管理端高危授权-普通用户越权访问管理功能失败
- 模块：认证登录/管理端高危授权
- 步骤：1. 以用户身份访问 POST /admin/auth/action-grant
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0001 用户信息查询-正常主路径成功
- 模块：用户中心/用户信息查询
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/user/info 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0002 用户信息查询-未登录访问被拒绝
- 模块：用户中心/用户信息查询
- 步骤：1. 不带鉴权调用/打开 GET /front/user/info
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0003 用户信息更新-正常主路径成功
- 模块：用户中心/用户信息更新
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/info 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0004 用户信息更新-未登录访问被拒绝
- 模块：用户中心/用户信息更新
- 步骤：1. 不带鉴权调用/打开 POST /front/user/info
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0012 余额流水-正常主路径成功
- 模块：用户中心/余额流水
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/user/balance/logs 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0013 余额流水-未登录访问被拒绝
- 模块：用户中心/余额流水
- 步骤：1. 不带鉴权调用/打开 GET /front/user/balance/logs
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0014 余额流水-重复提交幂等不双花
- 模块：用户中心/余额流水
- 步骤：1. 成功执行一次 2. 用相同业务键立即再执行一次 GET /front/user/balance/logs
- 输入：-
- 预期：第二次忽略或明确已处理；资金/库存不重复扣减或发放
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0015 余额流水-page=最小值0成功
- 模块：用户中心/余额流水
- 步骤：1. 以 page=0 调用 GET /front/user/balance/logs
- 输入：page=0
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0016 余额流水-page=min-1(-1)失败
- 模块：用户中心/余额流水
- 步骤：1. 调用 GET /front/user/balance/logs
- 输入：page=-1
- 预期：拒绝；提示数值范围
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0017 余额流水-page=最大值1000成功
- 模块：用户中心/余额流水
- 步骤：1. 调用 GET /front/user/balance/logs
- 输入：page=1000
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0018 余额流水-page=max+1(1001)失败
- 模块：用户中心/余额流水
- 步骤：1. 调用 GET /front/user/balance/logs
- 输入：page=1001
- 预期：拒绝；提示超限
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0019 余额流水-size=最小值1成功
- 模块：用户中心/余额流水
- 步骤：1. 以 size=1 调用 GET /front/user/balance/logs
- 输入：size=1
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0020 余额流水-size=min-1(0)失败
- 模块：用户中心/余额流水
- 步骤：1. 调用 GET /front/user/balance/logs
- 输入：size=0
- 预期：拒绝；提示数值范围
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0021 余额流水-size=最大值100成功
- 模块：用户中心/余额流水
- 步骤：1. 调用 GET /front/user/balance/logs
- 输入：size=100
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0022 余额流水-size=max+1(101)失败
- 模块：用户中心/余额流水
- 步骤：1. 调用 GET /front/user/balance/logs
- 输入：size=101
- 预期：拒绝；提示超限
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0023 绑定手机-正常主路径成功
- 模块：用户中心/绑定手机
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/bind-phone 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0024 绑定手机-未登录访问被拒绝
- 模块：用户中心/绑定手机
- 步骤：1. 不带鉴权调用/打开 POST /front/user/bind-phone
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0028 绑定手机-phone合法手机号成功
- 模块：用户中心/绑定手机
- 步骤：1. 输入合法手机号执行 POST /front/user/bind-phone
- 输入：phone=有效号段
- 预期：通过校验
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0029 绑定手机-phone非法格式失败
- 模块：用户中心/绑定手机
- 步骤：1. 非法手机号提交
- 输入：phone=123/abcd/空
- 预期：格式错误提示；不继续
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0037 推送Token注册-正常主路径成功
- 模块：用户中心/推送Token注册
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/push-token 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0038 推送Token注册-未登录访问被拒绝
- 模块：用户中心/推送Token注册
- 步骤：1. 不带鉴权调用/打开 POST /front/user/push-token
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0045 推送Token注销-正常主路径成功
- 模块：用户中心/推送Token注销
- 步骤：1. 准备合法前置数据 2. 执行 DELETE /front/user/push-token 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0046 推送Token注销-未登录访问被拒绝
- 模块：用户中心/推送Token注销
- 步骤：1. 不带鉴权调用/打开 DELETE /front/user/push-token
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0048 通知偏好-正常主路径成功
- 模块：用户中心/通知偏好
- 步骤：1. 准备合法前置数据 2. 执行 PUT /front/user/notification-prefs 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0049 通知偏好-未登录访问被拒绝
- 模块：用户中心/通知偏好
- 步骤：1. 不带鉴权调用/打开 PUT /front/user/notification-prefs
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0052 管理端用户查询-未登录访问被拒绝
- 模块：用户中心/管理端用户查询
- 步骤：1. 不带鉴权调用/打开 POST /admin/user/query
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0053 管理端用户查询-普通用户越权访问管理功能失败
- 模块：用户中心/管理端用户查询
- 步骤：1. 以用户身份访问 POST /admin/user/query
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0054 管理端用户创建-正常主路径成功
- 模块：用户中心/管理端用户创建
- 步骤：1. 准备合法前置数据 2. 执行 POST /admin/user 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0055 管理端用户创建-未登录访问被拒绝
- 模块：用户中心/管理端用户创建
- 步骤：1. 不带鉴权调用/打开 POST /admin/user
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0056 管理端用户创建-普通用户越权访问管理功能失败
- 模块：用户中心/管理端用户创建
- 步骤：1. 以用户身份访问 POST /admin/user
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0060 管理端用户创建-phone合法手机号成功
- 模块：用户中心/管理端用户创建
- 步骤：1. 输入合法手机号执行 POST /admin/user
- 输入：phone=有效号段
- 预期：通过校验
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0061 管理端用户创建-phone非法格式失败
- 模块：用户中心/管理端用户创建
- 步骤：1. 非法手机号提交
- 输入：phone=123/abcd/空
- 预期：格式错误提示；不继续
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0067 管理端用户更新-正常主路径成功
- 模块：用户中心/管理端用户更新
- 步骤：1. 准备合法前置数据 2. 执行 PUT /admin/user 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0068 管理端用户更新-未登录访问被拒绝
- 模块：用户中心/管理端用户更新
- 步骤：1. 不带鉴权调用/打开 PUT /admin/user
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0069 管理端用户更新-普通用户越权访问管理功能失败
- 模块：用户中心/管理端用户更新
- 步骤：1. 以用户身份访问 PUT /admin/user
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0071 管理端用户删除-正常主路径成功
- 模块：用户中心/管理端用户删除
- 步骤：1. 准备合法前置数据 2. 执行 DELETE /admin/user 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0072 管理端用户删除-未登录访问被拒绝
- 模块：用户中心/管理端用户删除
- 步骤：1. 不带鉴权调用/打开 DELETE /admin/user
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0073 管理端用户删除-普通用户越权访问管理功能失败
- 模块：用户中心/管理端用户删除
- 步骤：1. 以用户身份访问 DELETE /admin/user
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0075 钱包对账-正常主路径成功
- 模块：用户中心/钱包对账
- 步骤：1. 准备合法前置数据 2. 执行 GET /admin/user/{id}/wallet/reconcile 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0076 钱包对账-未登录访问被拒绝
- 模块：用户中心/钱包对账
- 步骤：1. 不带鉴权调用/打开 GET /admin/user/{id}/wallet/reconcile
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0077 钱包对账-普通用户越权访问管理功能失败
- 模块：用户中心/钱包对账
- 步骤：1. 以用户身份访问 GET /admin/user/{id}/wallet/reconcile
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0078 钱包对账-资源不存在返回NotFind
- 模块：用户中心/钱包对账
- 步骤：1. 使用不存在的 ID 调用 GET /admin/user/{id}/wallet/reconcile
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0079 钱包对账-重复提交幂等不双花
- 模块：用户中心/钱包对账
- 步骤：1. 成功执行一次 2. 用相同业务键立即再执行一次 GET /admin/user/{id}/wallet/reconcile
- 输入：-
- 预期：第二次忽略或明确已处理；资金/库存不重复扣减或发放
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0080 年龄确认-正常主路径成功
- 模块：合规/年龄确认
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/compliance/confirm-age 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0081 年龄确认-未登录访问被拒绝
- 模块：合规/年龄确认
- 步骤：1. 不带鉴权调用/打开 POST /front/user/compliance/confirm-age
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0083 年龄查询-正常主路径成功
- 模块：合规/年龄查询
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/user/compliance/age 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0084 年龄查询-未登录访问被拒绝
- 模块：合规/年龄查询
- 步骤：1. 不带鉴权调用/打开 GET /front/user/compliance/age
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0085 消费限额查询-正常主路径成功
- 模块：合规/消费限额查询
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/user/compliance/spend-limit 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0086 消费限额查询-未登录访问被拒绝
- 模块：合规/消费限额查询
- 步骤：1. 不带鉴权调用/打开 GET /front/user/compliance/spend-limit
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0087 消费限额查询-重复提交幂等不双花
- 模块：合规/消费限额查询
- 步骤：1. 成功执行一次 2. 用相同业务键立即再执行一次 GET /front/user/compliance/spend-limit
- 输入：-
- 预期：第二次忽略或明确已处理；资金/库存不重复扣减或发放
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0088 消费限额偏好-正常主路径成功
- 模块：合规/消费限额偏好
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/compliance/spend-limit/preference 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0089 消费限额偏好-未登录访问被拒绝
- 模块：合规/消费限额偏好
- 步骤：1. 不带鉴权调用/打开 POST /front/user/compliance/spend-limit/preference
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0090 消费限额偏好-重复提交幂等不双花
- 模块：合规/消费限额偏好
- 步骤：1. 成功执行一次 2. 用相同业务键立即再执行一次 POST /front/user/compliance/spend-limit/preference
- 输入：-
- 预期：第二次忽略或明确已处理；资金/库存不重复扣减或发放
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0093 消费限额偏好-limitAmount=最小值0成功
- 模块：合规/消费限额偏好
- 步骤：1. 以 limitAmount=0 调用 POST /front/user/compliance/spend-limit/preference
- 输入：limitAmount=0
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0094 消费限额偏好-limitAmount=min-1(-1)失败
- 模块：合规/消费限额偏好
- 步骤：1. 调用 POST /front/user/compliance/spend-limit/preference
- 输入：limitAmount=-1
- 预期：拒绝；提示数值范围
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0095 消费限额偏好-limitAmount=最大值100000000成功
- 模块：合规/消费限额偏好
- 步骤：1. 调用 POST /front/user/compliance/spend-limit/preference
- 输入：limitAmount=100000000
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0096 消费限额偏好-limitAmount=max+1(100000001)失败
- 模块：合规/消费限额偏好
- 步骤：1. 调用 POST /front/user/compliance/spend-limit/preference
- 输入：limitAmount=100000001
- 预期：拒绝；提示超限
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0097 实名认证-正常主路径成功
- 模块：合规/实名认证
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/compliance/identity/verify 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0098 实名认证-未登录访问被拒绝
- 模块：合规/实名认证
- 步骤：1. 不带鉴权调用/打开 POST /front/user/compliance/identity/verify
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0113 隐私导出-未登录访问被拒绝
- 模块：合规/隐私导出
- 步骤：1. 不带鉴权调用/打开 GET /front/user/privacy/export
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0114 注销申请-正常主路径成功
- 模块：合规/注销申请
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/user/privacy/delete-request 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-USER-0115 注销申请-未登录访问被拒绝
- 模块：合规/注销申请
- 步骤：1. 不带鉴权调用/打开 POST /front/user/privacy/delete-request
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0001 地址保存-正常主路径成功
- 模块：收货地址/地址保存
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/address/save 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0002 地址保存-未登录访问被拒绝
- 模块：收货地址/地址保存
- 步骤：1. 不带鉴权调用/打开 POST /front/address/save
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0011 地址保存-phoneNumber合法手机号成功
- 模块：收货地址/地址保存
- 步骤：1. 输入合法手机号执行 POST /front/address/save
- 输入：phoneNumber=有效号段
- 预期：通过校验
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0012 地址保存-phoneNumber非法格式失败
- 模块：收货地址/地址保存
- 步骤：1. 非法手机号提交
- 输入：phoneNumber=123/abcd/空
- 预期：格式错误提示；不继续
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0024 地址查询-未登录访问被拒绝
- 模块：收货地址/地址查询
- 步骤：1. 不带鉴权调用/打开 POST /front/address/query
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0025 地址删除-正常主路径成功
- 模块：收货地址/地址删除
- 步骤：1. 准备合法前置数据 2. 执行 DELETE /front/address 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0026 地址删除-未登录访问被拒绝
- 模块：收货地址/地址删除
- 步骤：1. 不带鉴权调用/打开 DELETE /front/address
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0028 地址置顶-正常主路径成功
- 模块：收货地址/地址置顶
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/address/top 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0029 地址置顶-未登录访问被拒绝
- 模块：收货地址/地址置顶
- 步骤：1. 不带鉴权调用/打开 POST /front/address/top
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0031 地址管理页-正常主路径成功
- 模块：收货地址/地址管理页
- 步骤：1. 准备合法前置数据 2. 执行 Mobile addresses / address-form 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-ADDR-0032 地址管理页-未登录访问被拒绝
- 模块：收货地址/地址管理页
- 步骤：1. 不带鉴权调用/打开 Mobile addresses / address-form
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0001 盲盒详情-正常主路径成功
- 模块：盲盒目录/盲盒详情
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/mystery-box/{id} 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0002 盲盒详情-未登录访问被拒绝
- 模块：盲盒目录/盲盒详情
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/{id}
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0003 盲盒详情-资源不存在返回NotFind
- 模块：盲盒目录/盲盒详情
- 步骤：1. 使用不存在的 ID 调用 GET /front/mystery-box/{id}
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0004 盲盒列表-正常主路径成功
- 模块：盲盒目录/盲盒列表
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/mystery-box/query 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0005 盲盒列表-未登录访问被拒绝
- 模块：盲盒目录/盲盒列表
- 步骤：1. 不带鉴权调用/打开 POST /front/mystery-box/query
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0007 盲盒搜索-未登录访问被拒绝
- 模块：盲盒目录/盲盒搜索
- 步骤：1. 不带鉴权调用/打开 POST /front/mystery-box/search
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0012 新人专属-未登录访问被拒绝
- 模块：盲盒目录/新人专属
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/newcomer-offer
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0013 奖池仪表盘-正常主路径成功
- 模块：盲盒目录/奖池仪表盘
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/mystery-box/{id}/pool-dashboard 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0014 奖池仪表盘-未登录访问被拒绝
- 模块：盲盒目录/奖池仪表盘
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/{id}/pool-dashboard
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0015 奖池仪表盘-资源不存在返回NotFind
- 模块：盲盒目录/奖池仪表盘
- 步骤：1. 使用不存在的 ID 调用 GET /front/mystery-box/{id}/pool-dashboard
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0017 信任元数据-未登录访问被拒绝
- 模块：盲盒目录/信任元数据
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/{id}/trust-meta
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0020 洞察-未登录访问被拒绝
- 模块：盲盒目录/洞察
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/{id}/insight
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0023 盒中奖动态-未登录访问被拒绝
- 模块：盲盒目录/盒中奖动态
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/{id}/draw-feed
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0026 全局中奖动态-未登录访问被拒绝
- 模块：盲盒目录/全局中奖动态
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/draw-feed
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0027 保底进度-正常主路径成功
- 模块：盲盒目录/保底进度
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/mystery-box/{id}/pity-progress 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0028 保底进度-未登录访问被拒绝
- 模块：盲盒目录/保底进度
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/{id}/pity-progress
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0029 保底进度-资源不存在返回NotFind
- 模块：盲盒目录/保底进度
- 步骤：1. 使用不存在的 ID 调用 GET /front/mystery-box/{id}/pity-progress
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0030 保底补偿-正常主路径成功
- 模块：盲盒目录/保底补偿
- 步骤：1. 准备合法前置数据 2. 执行 POST /front/mystery-box/{id}/pity-compensate 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0031 保底补偿-未登录访问被拒绝
- 模块：盲盒目录/保底补偿
- 步骤：1. 不带鉴权调用/打开 POST /front/mystery-box/{id}/pity-compensate
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0032 保底补偿-资源不存在返回NotFind
- 模块：盲盒目录/保底补偿
- 步骤：1. 使用不存在的 ID 调用 POST /front/mystery-box/{id}/pity-compensate
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0033 保底补偿-重复提交幂等不双花
- 模块：盲盒目录/保底补偿
- 步骤：1. 成功执行一次 2. 用相同业务键立即再执行一次 POST /front/mystery-box/{id}/pity-compensate
- 输入：-
- 预期：第二次忽略或明确已处理；资金/库存不重复扣减或发放
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0034 概率公示-正常主路径成功
- 模块：盲盒目录/概率公示
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/mystery-box/{id}/probability 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0035 概率公示-未登录访问被拒绝
- 模块：盲盒目录/概率公示
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/{id}/probability
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0036 概率公示-资源不存在返回NotFind
- 模块：盲盒目录/概率公示
- 步骤：1. 使用不存在的 ID 调用 GET /front/mystery-box/{id}/probability
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0038 概率历史-未登录访问被拒绝
- 模块：盲盒目录/概率历史
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/{id}/probability/history
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0041 连拍配置-未登录访问被拒绝
- 模块：盲盒目录/连拍配置
- 步骤：1. 不带鉴权调用/打开 GET /front/mystery-box/draw-pack-configs
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0042 管理端保存盲盒-正常主路径成功
- 模块：盲盒目录/管理端保存盲盒
- 步骤：1. 准备合法前置数据 2. 执行 POST /admin/mystery-box/save 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0043 管理端保存盲盒-未登录访问被拒绝
- 模块：盲盒目录/管理端保存盲盒
- 步骤：1. 不带鉴权调用/打开 POST /admin/mystery-box/save
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0044 管理端保存盲盒-普通用户越权访问管理功能失败
- 模块：盲盒目录/管理端保存盲盒
- 步骤：1. 以用户身份访问 POST /admin/mystery-box/save
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0045 管理端保存盲盒-重复提交幂等不双花
- 模块：盲盒目录/管理端保存盲盒
- 步骤：1. 成功执行一次 2. 用相同业务键立即再执行一次 POST /admin/mystery-box/save
- 输入：-
- 预期：第二次忽略或明确已处理；资金/库存不重复扣减或发放
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0053 管理端保存盲盒-price=最小值1成功
- 模块：盲盒目录/管理端保存盲盒
- 步骤：1. 以 price=1 调用 POST /admin/mystery-box/save
- 输入：price=1
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0054 管理端保存盲盒-price=min-1(0)失败
- 模块：盲盒目录/管理端保存盲盒
- 步骤：1. 调用 POST /admin/mystery-box/save
- 输入：price=0
- 预期：拒绝；提示数值范围
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0055 管理端保存盲盒-price=最大值10000000成功
- 模块：盲盒目录/管理端保存盲盒
- 步骤：1. 调用 POST /admin/mystery-box/save
- 输入：price=10000000
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0056 管理端保存盲盒-price=max+1(10000001)失败
- 模块：盲盒目录/管理端保存盲盒
- 步骤：1. 调用 POST /admin/mystery-box/save
- 输入：price=10000001
- 预期：拒绝；提示超限
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0057 设置保底阈值-正常主路径成功
- 模块：盲盒目录/设置保底阈值
- 步骤：1. 准备合法前置数据 2. 执行 PUT /admin/mystery-box/{id}/pity-threshold 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0058 设置保底阈值-未登录访问被拒绝
- 模块：盲盒目录/设置保底阈值
- 步骤：1. 不带鉴权调用/打开 PUT /admin/mystery-box/{id}/pity-threshold
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0059 设置保底阈值-普通用户越权访问管理功能失败
- 模块：盲盒目录/设置保底阈值
- 步骤：1. 以用户身份访问 PUT /admin/mystery-box/{id}/pity-threshold
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0060 设置保底阈值-资源不存在返回NotFind
- 模块：盲盒目录/设置保底阈值
- 步骤：1. 使用不存在的 ID 调用 PUT /admin/mystery-box/{id}/pity-threshold
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0069 低库存告警-未登录访问被拒绝
- 模块：盲盒目录/低库存告警
- 步骤：1. 不带鉴权调用/打开 GET /admin/mystery-box/low-stock-alerts
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0070 低库存告警-普通用户越权访问管理功能失败
- 模块：盲盒目录/低库存告警
- 步骤：1. 以用户身份访问 GET /admin/mystery-box/low-stock-alerts
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0071 库存审计-正常主路径成功
- 模块：盲盒目录/库存审计
- 步骤：1. 准备合法前置数据 2. 执行 GET /admin/prize-stock-audit 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0072 库存审计-未登录访问被拒绝
- 模块：盲盒目录/库存审计
- 步骤：1. 不带鉴权调用/打开 GET /admin/prize-stock-audit
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0073 库存审计-普通用户越权访问管理功能失败
- 模块：盲盒目录/库存审计
- 步骤：1. 以用户身份访问 GET /admin/prize-stock-audit
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0074 管理端盲盒CRUD页-正常主路径成功
- 模块：盲盒目录/管理端盲盒CRUD页
- 步骤：1. 准备合法前置数据 2. 执行 Admin /mystery-box 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0075 管理端盲盒CRUD页-未登录访问被拒绝
- 模块：盲盒目录/管理端盲盒CRUD页
- 步骤：1. 不带鉴权调用/打开 Admin /mystery-box
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0076 管理端盲盒CRUD页-普通用户越权访问管理功能失败
- 模块：盲盒目录/管理端盲盒CRUD页
- 步骤：1. 以用户身份访问 Admin /mystery-box
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0080 管理端商品CRUD页-正常主路径成功
- 模块：盲盒目录/管理端商品CRUD页
- 步骤：1. 准备合法前置数据 2. 执行 Admin /product 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0081 管理端商品CRUD页-未登录访问被拒绝
- 模块：盲盒目录/管理端商品CRUD页
- 步骤：1. 不带鉴权调用/打开 Admin /product
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0082 管理端商品CRUD页-普通用户越权访问管理功能失败
- 模块：盲盒目录/管理端商品CRUD页
- 步骤：1. 以用户身份访问 Admin /product
- 输入：-
- 预期：403/无权限；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0086 移动端盒详情下单页-正常主路径成功
- 模块：盲盒目录/移动端盒详情下单页
- 步骤：1. 准备合法前置数据 2. 执行 Mobile box/{id} 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0087 移动端盒详情下单页-未登录访问被拒绝
- 模块：盲盒目录/移动端盒详情下单页
- 步骤：1. 不带鉴权调用/打开 Mobile box/{id}
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0088 移动端盒详情下单页-重复提交幂等不双花
- 模块：盲盒目录/移动端盒详情下单页
- 步骤：1. 成功执行一次 2. 用相同业务键立即再执行一次 Mobile box/{id}
- 输入：-
- 预期：第二次忽略或明确已处理；资金/库存不重复扣减或发放
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0091 移动端盒详情下单页-drawCount=最小值1成功
- 模块：盲盒目录/移动端盒详情下单页
- 步骤：1. 以 drawCount=1 调用 Mobile box/{id}
- 输入：drawCount=1
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0092 移动端盒详情下单页-drawCount=min-1(0)失败
- 模块：盲盒目录/移动端盒详情下单页
- 步骤：1. 调用 Mobile box/{id}
- 输入：drawCount=0
- 预期：拒绝；提示数值范围
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0093 移动端盒详情下单页-drawCount=最大值100成功
- 模块：盲盒目录/移动端盒详情下单页
- 步骤：1. 调用 Mobile box/{id}
- 输入：drawCount=100
- 预期：成功或按业务允许
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0094 移动端盒详情下单页-drawCount=max+1(101)失败
- 模块：盲盒目录/移动端盒详情下单页
- 步骤：1. 调用 Mobile box/{id}
- 输入：drawCount=101
- 预期：拒绝；提示超限
- 结果：☐通过 ☐失败 ☐阻塞

## B-BOX-0103 移动端盒详情下单页-弱网超时重试不双提交
- 模块：盲盒目录/移动端盒详情下单页
- 步骤：1. 弱网点击提交 2. 超时后再次提交
- 输入：-
- 预期：有防重复；最终状态唯一正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0001 加入队列-正常主路径成功
- 模块：抽赏队列库存/加入队列
- 步骤：1. 准备合法前置数据 2. 执行 POST .../draw-queue/join 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0002 加入队列-未登录访问被拒绝
- 模块：抽赏队列库存/加入队列
- 步骤：1. 不带鉴权调用/打开 POST .../draw-queue/join
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0004 队列状态-正常主路径成功
- 模块：抽赏队列库存/队列状态
- 步骤：1. 准备合法前置数据 2. 执行 GET .../draw-queue/status 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0005 队列状态-未登录访问被拒绝
- 模块：抽赏队列库存/队列状态
- 步骤：1. 不带鉴权调用/打开 GET .../draw-queue/status
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0006 队列续租-正常主路径成功
- 模块：抽赏队列库存/队列续租
- 步骤：1. 准备合法前置数据 2. 执行 POST .../draw-queue/renew 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0007 队列续租-未登录访问被拒绝
- 模块：抽赏队列库存/队列续租
- 步骤：1. 不带鉴权调用/打开 POST .../draw-queue/renew
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0009 离开队列-正常主路径成功
- 模块：抽赏队列库存/离开队列
- 步骤：1. 准备合法前置数据 2. 执行 DELETE .../draw-queue/leave 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0010 离开队列-未登录访问被拒绝
- 模块：抽赏队列库存/离开队列
- 步骤：1. 不带鉴权调用/打开 DELETE .../draw-queue/leave
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0012 获取全收锁-正常主路径成功
- 模块：抽赏队列库存/获取全收锁
- 步骤：1. 准备合法前置数据 2. 执行 POST .../draw-queue/buyout-lock 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0013 获取全收锁-未登录访问被拒绝
- 模块：抽赏队列库存/获取全收锁
- 步骤：1. 不带鉴权调用/打开 POST .../draw-queue/buyout-lock
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0015 释放全收锁-正常主路径成功
- 模块：抽赏队列库存/释放全收锁
- 步骤：1. 准备合法前置数据 2. 执行 DELETE .../draw-queue/buyout-lock 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0016 释放全收锁-未登录访问被拒绝
- 模块：抽赏队列库存/释放全收锁
- 步骤：1. 不带鉴权调用/打开 DELETE .../draw-queue/buyout-lock
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0018 续租全收锁-正常主路径成功
- 模块：抽赏队列库存/续租全收锁
- 步骤：1. 准备合法前置数据 2. 执行 POST .../draw-queue/buyout-lock/renew 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0019 续租全收锁-未登录访问被拒绝
- 模块：抽赏队列库存/续租全收锁
- 步骤：1. 不带鉴权调用/打开 POST .../draw-queue/buyout-lock/renew
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0021 全收锁状态-正常主路径成功
- 模块：抽赏队列库存/全收锁状态
- 步骤：1. 准备合法前置数据 2. 执行 GET .../draw-queue/buyout-lock 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0022 全收锁状态-未登录访问被拒绝
- 模块：抽赏队列库存/全收锁状态
- 步骤：1. 不带鉴权调用/打开 GET .../draw-queue/buyout-lock
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0023 柜位列表-正常主路径成功
- 模块：抽赏队列库存/柜位列表
- 步骤：1. 准备合法前置数据 2. 执行 GET .../slots 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0024 柜位列表-未登录访问被拒绝
- 模块：抽赏队列库存/柜位列表
- 步骤：1. 不带鉴权调用/打开 GET .../slots
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0025 预约柜位-正常主路径成功
- 模块：抽赏队列库存/预约柜位
- 步骤：1. 准备合法前置数据 2. 执行 POST .../slots/{slotNo}/reserve 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0026 预约柜位-未登录访问被拒绝
- 模块：抽赏队列库存/预约柜位
- 步骤：1. 不带鉴权调用/打开 POST .../slots/{slotNo}/reserve
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0028 释放柜位-正常主路径成功
- 模块：抽赏队列库存/释放柜位
- 步骤：1. 准备合法前置数据 2. 执行 DELETE .../slots/reserve 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0029 释放柜位-未登录访问被拒绝
- 模块：抽赏队列库存/释放柜位
- 步骤：1. 不带鉴权调用/打开 DELETE .../slots/reserve
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0031 提示会话-正常主路径成功
- 模块：抽赏队列库存/提示会话
- 步骤：1. 准备合法前置数据 2. 执行 GET .../hint/session 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0032 提示会话-未登录访问被拒绝
- 模块：抽赏队列库存/提示会话
- 步骤：1. 不带鉴权调用/打开 GET .../hint/session
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0033 使用提示-正常主路径成功
- 模块：抽赏队列库存/使用提示
- 步骤：1. 准备合法前置数据 2. 执行 POST .../hint 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0034 使用提示-未登录访问被拒绝
- 模块：抽赏队列库存/使用提示
- 步骤：1. 不带鉴权调用/打开 POST .../hint
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0036 公平信标-正常主路径成功
- 模块：抽赏队列库存/公平信标
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/fairness/daily-beacon 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0037 公平信标-未登录访问被拒绝
- 模块：抽赏队列库存/公平信标
- 步骤：1. 不带鉴权调用/打开 GET /front/fairness/daily-beacon
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0038 抽赏日志-正常主路径成功
- 模块：抽赏队列库存/抽赏日志
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/fairness/draw-log/{id} 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0039 抽赏日志-未登录访问被拒绝
- 模块：抽赏队列库存/抽赏日志
- 步骤：1. 不带鉴权调用/打开 GET /front/fairness/draw-log/{id}
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0040 抽赏日志-资源不存在返回NotFind
- 模块：抽赏队列库存/抽赏日志
- 步骤：1. 使用不存在的 ID 调用 GET /front/fairness/draw-log/{id}
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0041 订单公平证明-正常主路径成功
- 模块：抽赏队列库存/订单公平证明
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/fairness/order/{orderId} 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0042 订单公平证明-未登录访问被拒绝
- 模块：抽赏队列库存/订单公平证明
- 步骤：1. 不带鉴权调用/打开 GET /front/fairness/order/{orderId}
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0043 订单公平证明-资源不存在返回NotFind
- 模块：抽赏队列库存/订单公平证明
- 步骤：1. 使用不存在的 ID 调用 GET /front/fairness/order/{orderId}
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0044 公平commit-正常主路径成功
- 模块：抽赏队列库存/公平commit
- 步骤：1. 准备合法前置数据 2. 执行 GET /front/fairness/order/{orderId}/commit 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0045 公平commit-未登录访问被拒绝
- 模块：抽赏队列库存/公平commit
- 步骤：1. 不带鉴权调用/打开 GET /front/fairness/order/{orderId}/commit
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0046 公平commit-资源不存在返回NotFind
- 模块：抽赏队列库存/公平commit
- 步骤：1. 使用不存在的 ID 调用 GET /front/fairness/order/{orderId}/commit
- 输入：id=not-exist-uuid
- 预期：业务 NotFind/404；文案明确；无 500
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0047 创建中奖规则-正常主路径成功
- 模块：抽赏队列库存/创建中奖规则
- 步骤：1. 准备合法前置数据 2. 执行 POST /admin/mystery-box-win-rule/create 3. 校验响应/页面/DB
- 输入：全部必填合法等价类
- 预期：成功码/成功态；数据落库或状态变更正确；页面提示正确
- 结果：☐通过 ☐失败 ☐阻塞

## B-DRAW-0048 创建中奖规则-未登录访问被拒绝
- 模块：抽赏队列库存/创建中奖规则
- 步骤：1. 不带鉴权调用/打开 POST /admin/mystery-box-win-rule/create
- 输入：-
- 预期：401 或跳转登录；无数据变更
- 结果：☐通过 ☐失败 ☐阻塞

